# IRCTC Conversational IVR Modernization Framework
# Python dependencies — Milestone 2 backend + Milestone 4 tests
#
# Install all:
#   pip install -r requirements.txt

# ── Backend (Milestone 2) ──────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
requests>=2.31.0

# ── Testing (Milestone 4) ─────────────────────────
pytest>=7.4.0
httpx>=0.24.0          # required by FastAPI TestClient

# ── Optional: Azure ACS SDK (Milestone 4 production) ──
# azure-communication-callautomation>=1.1.0

# ── Optional: Twilio SDK (Milestone 4 production) ─────
# twilio>=8.0.0